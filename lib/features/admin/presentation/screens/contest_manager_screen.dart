import 'package:flutter/material.dart';

/// Contest manager admin screen placeholder - full implementation in FEAT-004.
class ContestManagerScreen extends StatelessWidget {
  const ContestManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Contest Manager Screen'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

/// Match manager admin screen placeholder - full implementation in FEAT-004.
class MatchManagerScreen extends StatelessWidget {
  const MatchManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Match Manager Screen'),
      ),
    );
  }
}

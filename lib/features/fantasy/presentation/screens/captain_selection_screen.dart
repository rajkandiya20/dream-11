import 'package:flutter/material.dart';

/// Captain selection screen placeholder - full implementation in FEAT-004.
class CaptainSelectionScreen extends StatelessWidget {
  const CaptainSelectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Captain Selection Screen'),
      ),
    );
  }
}
